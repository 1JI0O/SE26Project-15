[Unit]
Description=TraceLab external PostgreSQL and blob backup
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=/opt/tracelab/server
ExecStart=/usr/bin/docker compose --env-file .env --profile maintenance run --rm backup
